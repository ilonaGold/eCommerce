/*import { getAnonymousAccessTokenData } from "../../auth/getAnonymousAccessToken";
import { getCustomerAccessTokenData } from "../../auth/getCustomerAccessToken";
import { isCustomerLoggedIn } from "../../auth/isCustomerLoggedIn";

export const checkMyActiveCart = async (): Promise<Response> => {
  const url = `${import.meta.env.VITE_CTP_AUTH_URL}/${import.meta.env.VITE_CTP_PROJECT_KEY}/me/active-cart`;
}
*/
